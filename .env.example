# Optional public production origin for canonical metadata, Open Graph, robots,
# and sitemap. Blank or invalid values safely fall back to Vercel's deployment
# URL and then to https://zema.hisab.et.
NEXT_PUBLIC_SITE_URL=https://zema.example.com
